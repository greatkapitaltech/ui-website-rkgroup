<?php
$servername = "localhost";
$username = "ddecou4l_ddecor";
$password = "xm&6#t]C~R7c";
$dbname = "ddecou4l_ddecor";

// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);

// Check connection
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}

$sql = "SELECT id, product_title FROM products";
$result = $conn->query($sql);

if ($result->num_rows > 0) {
    while($row = $result->fetch_assoc()) {
        $productId = $row["id"];
        $productTitle = $row["product_title"];
        $updatedProductTitle = preg_replace('/\d+/', '', $productTitle);
        
        $updateSql = "UPDATE products SET product_title='$updatedProductTitle' WHERE id=$productId";
        if ($conn->query($updateSql) === TRUE) {
            echo "Record updated successfully for product with ID $productId<br>";
        } else {
            echo "Error updating record: " . $conn->error;
        }
    }
} else {
    echo "0 results";
}

$conn->close();
?>